package org.example;

/**
 * A simple class to store a position in a 2D grid, as a tuple of (row, column).
 */
public class Position {
    public int row;
    public int column;

    public Position(int row, int column) {
        this.row = row;
        this.column = column;
    }
}